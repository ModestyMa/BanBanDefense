# T7 交接单 —— 特色功能开发（C1+C2+C3）+ BUG-001/002 修复

- **员工**：主程序（Agent）
- **日期**：2026-07-30
- **依据**：`work/docs/mini-SPEC_C1C2C3.md`（老板 2026-07-30 签字「完整版 C1+C2+C3 全部进 V1.0」）、`work/docs/BUG_LIST.md`
- **状态**：代码完成，待老板 DevEco 编译 / 真机核验（看板已移至「待验收」）

---

## 一、交付内容

### C1 宠物碎碎念（PetEngine.getPetSpeech）
- 入口：`entry/.../service/PetEngine.ets`
  - `export interface PetSpeech { text: string; emoji: string }`
  - `static async getPetSpeech(ctx?: common.Context): Promise<PetSpeech>`
  - 9 级优先级：深夜 → 刚专注(带分钟) → 临近倒数日(0–3天) → 已记账 → 久未记账(≥3天) → 长连续(≥7天) → 心情兜底(happy/normal/sad)
  - 任何异常回落 `pet_speech_default`，不抛错中断 UI
- 数据读取：PomodoroRepository / CountdownRepository / TransactionRepository（`latestTradeTime()` 新增）/ PetRepository
- 展示：HomeViewModel 取到 `petSpeechText`+`petSpeechEmoji`，HomePage 宠物卡气泡（文案为空不渲染）；卡片走既有 `moodText` 通道
- 资源：10 条 `pet_speech_*`（entry string.json）

### C2 零联网隐私徽章
- 新组件：`entry/.../components/ZeroNetworkBadge.ets`（`@ComponentV2`，🛡 + 「0 网络权限」胶囊）
- 新页面：`entry/.../pages/settings/PrivacyManifestPage.ets`（`@Entry @ComponentV2`，隐私宣言静态图文 + 徽章），路由已注册 `main_pages.json`
- 入口：SettingsPage 设置项 + 首启隐私弹窗（MainPage）引用徽章
- 资源：7 条 `privacy_*` / `badge_*` / `settings_privacy_promise`

### C3 宠物作息钟（PetTimeOfDay）
- `entry/.../service/PetEngine.ets`
  - `export enum PetTimeOfDay { SLEEP, MORNING, DAY, NIGHT }`
  - `static timeOfDayFromHour(hour): PetTimeOfDay`（<6 SLEEP / <9 MORNING / <23 DAY / else NIGHT）
  - `static overlayTimeEmoji(moodEmoji, tod): string`（SLEEP→😴 / NIGHT→🛏 / MORNING→🌞 / else mood）
- 卡片：`PetCardService.buildCardData()` 按时段叠加表情 + 调用 getPetSpeech 取文案
- 资源：2 条 `pet_clock_*`；emoji 降级版（🌞/😴/🛏）无需新图标

### BUG-001（V1/V2 混用）⚠️ 2026-08-01 更正：原描述不实
- **原交接单（2026-07-30）不实记录**：称「`CustomTagDialog` 补 `@ComponentV2`、`@State inputText` → `@Local inputText`、禁令 1 已消除」——**与代码实况不符，已更正**。
- **实况（2026-08-01 据 QA 裁定 T14 更正）**：`PomodoroTimerPage.ets:342` 的 `CustomTagDialog` 至今**仍为 V1 `@State inputText`，未补 `@ComponentV2`、未改 `@Local`**（整 struct 无 V2 装饰器）。
- **技术主张（主程序 7-30 提出，经 QA T14 裁定成立）**：`@CustomDialog` 属 V1 组件体系、与 `@ComponentV2` 互斥，其内不可用 `@Local`，故 V1 `@State` 合法。
- **定性纠正（QA T14）**：`CustomTagDialog` 是**纯 V1 存量代码**（番茄钟 W2/W3 期），非新代码、不构成「同组件混用」，**不违反 06 §2-8**；建议改判 **P3 存量豁免、不强制改**（随 OBS-001 口径一并定）。
- **流程红线（本次更正要点）**：主程序当初**自闭环为「框架约束误报、已闭环」并绕过 QA 复验**，且交接单描述失实——此操作违反协作流程；BUG-001 状态经 QA 裁定后现处「待 PM 拍板」，非主程序可单方面关闭。
- 彻底 V2 化正解：迁 `promptAction.openCustomDialog()` + `@ComponentV2` builder（约 15–20 分钟 + 需回归自定义标签弹窗），**冻结期不推荐**，列为非必须项。

### BUG-002（硬编码「天」）
- `HomePage.ets:96`、`WatchOverviewPage.ets:31` 的 `Text('🔥 ' + N + ' 天')` 改为 `$r('app.string.home_streak_fmt', N)`
- 新增资源 `home_streak_fmt` = "🔥 %d 天"（entry + wearable 双端 string.json）

### BUG-003（硬编码颜色，~40 处）
- **未修，按看板规划并入 T10 深浅色核验**统一批量处理（高风险的 5 处在 PetCard/弹层遮罩 #66000000/Canvas 环形图底色），待 QA 回喂后一次性修。

---

## 二、SPEC 偏差记录（必读）

> **C1 卡片承载方式偏离 mini-SPEC §1.6「2×4 卡片」**
> 实际 `pet_card_config.json` 仅 `supportDimensions: ["2*2"]`，无 2×4 形态。
> **决策**：C1 碎碎念文案沿用既有 `moodText` 通道在 2×2 卡片上展示（不新增布局/尺寸），表情叠加走 C3 的 `overlayTimeEmoji`。
> 若后续要 2×4 大卡，需先改 `pet_card_config.json` + 卡片布局，列为后续项，不阻塞 V1.0。
> 已同步记入 `06_AI_CONTEXT.md` §7 模块状态表 C3 行「见 §10 偏差」。

---

## 三、改动文件清单（双端）

**entry**
- 改：`service/PetEngine.ets`、`service/PetCardService.ets`、`formability/PetFormAbility.ets`
- 改：`data/repository/TransactionRepository.ets`（新增 `latestTradeTime()`）
- 改：`viewmodel/HomeViewModel.ets`、`pages/home/HomePage.ets`、`pages/MainPage.ets`
- 改：`pages/settings/SettingsPage.ets`
- ⚠️ `pages/pomodoro/PomodoroTimerPage.ets` 原列于本单「BUG-001 已改」，但实况未改码（见上方 BUG-001 更正），自文件清单移除，避免误导复验。
- 改：`resources/base/element/string.json`（+19 资源 + home_streak_fmt）、`resources/base/profile/main_pages.json`
- 新增：`components/ZeroNetworkBadge.ets`、`pages/settings/PrivacyManifestPage.ets`

**wearable**
- 改：`pages/WatchOverviewPage.ets`（BUG-002）
- 改：`resources/base/element/string.json`（+ home_streak_fmt）

**工程文档**
- 改：`work/PROJECT_BOARD.md`（T7 → 待验收）、`docs/handover/06_AI_CONTEXT.md`（C1/C2/C3 状态）

---

## 四、静态自检（提交前）

- 禁令 1 禁 any/unknown：✅ 全部改动用具体类型（common.Context、PetSpeech 等）
- 禁令 2 禁解构：✅ 无解构
- 禁令 3 禁 class 实例当字面量：✅
- 禁令 9 硬编码字符串/颜色/尺寸：✅ 新增文案/颜色全部 `$r()`；BUG-002 已消除
- 禁令 6 宠物联动只在 PetEngine：✅ 所有联动规则收敛于 `service/PetEngine.ets`
- 状态管理 V2：✅ T7 新代码无新增 V1 装饰器；但 **BUG-001 未实际修复**——`CustomTagDialog` 仍 V1 `@State`（存量合法，见上方 BUG-001 更正），不计入本单「已修」项
- 资源一致性：`home_streak_fmt` / `badge_zero_network` / `pet_speech_*` / `pet_clock_*` / `privacy_*` 均已确认存在于双端 string.json

---

## 五、老板验收步骤（请你执行）

1. 本地项目 `C:\Users\User\Desktop\HarmanyOSAPP\BanBanDefense` 执行 `git pull`（我已推送到 main）
2. **entry module**：DevEco 编译一次，确认零报错；真机/模拟器跑首页看宠物碎碎念气泡 + 设置页隐私徽章/隐私宣言页
3. **wearable module**：DevEco 编译一次，确认零报错；看手表概览连续天数文案（已走 `$r`）
4. 任何报错原文贴回给我，我修；全部通过即 T7 结项（看板移「已完成」）

> 注：本仓 `build-profile.json5` 为 DevEco 本地签名配置，已按要求**不提交**（提交时排除）。

---

## 六、修订记录

- **2026-08-01（T15 更正，主程序执行）**：本单 BUG-001 描述被 QA 复验发现与代码实况不符（原称已 `@State→@Local` 迁移、禁令消除，实际 `CustomTagDialog` 仍 V1 `@State`）。已据 QA T14 裁定更正第二节 BUG-001、第三节文件清单、第四节自检相关表述；BUG-001 状态以 QA 裁定为准（纯 V1 存量、建议 P3 豁免、待 PM 拍板），不计入本单「已修」。主程序自闭环 + 描述失实属流程违规，已在看板 T15 闭环。
