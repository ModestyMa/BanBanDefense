# T22 PC 端（2in1）全功能验证待验清单（老板照做版）

> 维护：QA（2026-08-03）｜ 依据：CHECKLIST_M6 B 区 + C 区 + D 区 + F-2（PC 深浅色）
> 范围说明：T22 不是只验 D 区布局——PC 端共用 entry module，功能与手机完全一致，须做**全功能验证**：B 区功能 + C/D 布局 + F-2 深浅色，在 PC 窗口形态下整体跑一遍。
> 执行方式：**本机（WorkBuddy 容器）无 hdc/ohpm/hvigor/java/系统 bm，QA 无法 build/install/UI 自动化，转老板手动**。已备现成包 `entry/build/default/outputs/default/entry-default-signed.hap`，可直接 `hdc install` 到本鸿蒙 PC。

---

## 0. 前置：装包与起 App（约 2 分钟）

- [ ] **0-1** 用 `hdc install entry/build/default/outputs/default/entry-default-signed.hap` 把已签名包装到本鸿蒙 PC（或 DevEco 直连本机 Run）。预期：安装成功，桌面/应用列表出现「伴伴」图标。
- [ ] **0-2** 启动「伴伴」，确认进入首页（非白屏、非永久空白）。预期：首页三卡（宠物/番茄/记账）均有真实数据或初始态，无 BUG-004 类白屏。
- [ ] **0-3** 确认应用为**可拖拽自由窗口**（2in1 形态），非强制全屏。预期：窗口可拖动边缘缩放。

## A. 出包（老板免 build，已备包）

- [ ] **A-PC** 出包已在 `entry-default-signed.hap` 完成。如须重出：DevEco `Build APP(s)` 选 phone/tablet/**2in1** 应成功（A-8 三项覆盖）。预期：2in1 在编译目标内、无报错。

## B. PC 端功能验证（窗口形态下逐个跑，等同手机全功能）

> PC 导航形态为左侧竖排（非手机底部），B-1 的「底部」在 PC 不适用，改为「导航可达 4 区」。

- [ ] **B-1** 左侧导航可到达 4 区：首页 / 倒数日 / 番茄 / 记账。预期：点击切换正常，无死链。
- [ ] **B-2** 首页三卡呈现：窗口较宽时**横排**（宠物卡 → 今日番茄 → 本月收支）；拖到窄时转竖排。预期：随窗口宽度自适应，无居中限宽错乱。
- [ ] **B-3** 点「首页」导航项进首页（不误进记账）。预期：进入首页。
- [ ] **B-4** 倒数日：列表 / 新增 / 编辑 / 删除正常。**注意 D-013 已取消 3 限**（T17 落地），新建**无上限**、不弹「Pro 即将上线」。预期：新增 5 条以上仍可成功、编辑/删除生效。
- [ ] **B-5** 番茄钟：开始 / 暂停 / 完成；完成 1 个 `exp +2`（回首页/宠物区看经验变化）。预期：计时与经验正确。
- [ ] **B-6** 记账：记 1 笔 `exp +1`、余额变动正确；进入记账报表页布局正常（PC 宽屏应为左右分栏）。预期：数字与报表无溢出/重叠。
- [ ] **B-7** 设置页：隐私政策可打开；「清除全部数据」可用。预期：点开隐私页正常；清除后回到初始态。
- [ ] **B-8** 记账浮层（页面内浮层降级版）弹出 / 关闭 / 保存正常。预期：浮层交互无卡死。

## C/D. 布局与窗口拖拽（PC 专属，核心）

- [ ] **D-1** 左侧竖排导航可用，4 Tab 切换正常。预期：导航高亮与内容同步。
- [ ] **D-2** **拖拽窗口边缘缩放**，跨过 **640vp** 与 **840vp** 两个断点时布局切换流畅、无错乱、无闪烁死循环。预期：断点切换一次到位，不来回跳。
- [ ] **D-3** 窗口拖到最窄（<640vp）时回到**手机式底部导航**布局。预期：左导航消失、底部 Tab 出现。
- [ ] **D-4** 窗口拖宽后首页三卡**横排**、报表**分栏**正确出现。预期：宽屏下内容合理分栏，不拉伸变形。
- [ ] **C-2** 宽屏内容居中限宽（最大 640vp 内容区 + 两侧留白均匀），或按 SPEC 分栏；不拉伸铺满。预期：视觉舒适、不变形。
- [ ] **C-4** 记账报表左右分栏（lg 下），无重叠无溢出。预期：左筛选/右图表分明。

## F. PC 深浅色

- [ ] **F-2** PC 深/浅切换正常：系统切深色后，App 卡片底色与文字对比度正常、无 BUG-003 类深色事故。预期：深色模式无视觉事故（BUG-003 已随 T10/T16 关闭，此处回归确认）。

## T12 补充：PC 端冷启动 / 核心路径（T12 整包回归未含 PC，此处补）

- [ ] **PC-冷启** PC 冷启动 < 1.5s，首页三卡正常（确认 BUG-004 在 PC 端无白屏）。预期：首屏数据即时呈现。
- [ ] **PC-核心** 核心路径（倒数日新增 → 番茄完成 → 记账 → 报表）零崩溃。预期：连续操作无 ANR/闪退。
- [ ] **PC-数据** 卸载重装数据清空；覆盖安装（升级）数据保留。预期：与 T12 结论一致（如方便可用旧版本包验证升级保留）。

---

## 降级预案（SPEC_M6 §9，不阻塞）

- 若 D-1/D-2 左导航样式怪异 → 降级为「宽屏保持底部导航 + 限宽容器」，报告 QA 即可，不重开方案。
- 若自由多窗在您环境不可用 → 用 DevEco 平板模拟器开自由多窗替代；仍不行则只验单窗口宽屏形态并标注。

## 回报格式（验完贴给 QA）

```
核验环境：本鸿蒙 PC / DevEco 版本 / 自由多窗是否可用
未通过项编号：如 D-2、B-4
现象：（一句话）
截图/录屏：（如有可能附上）
```

> QA 收到后：登记/更新 Bug（如需）、更新看板 T22 状态、写 T22 交接单。
