# QA 静态走查报告 — 06 §2 硬性禁令全仓扫描

- 日期：2026-07-30 ｜ 角色：QA ｜ 范围：entry/src + wearable/src 全部 .ets（68 文件）
- 性质：独立复扫（不依赖主程序 T1 预检结论），逐条对照 06_AI_CONTEXT §2

## 逐条结论

| 禁令 | 扫描方式 | 结论 |
|---|---|---|
| 1. any/unknown | `: any / as any / : unknown / <unknown>` 正则 | ✅ 零命中（与 T1 预检一致） |
| 2. 解构赋值 | `const {x / let {x` 正则 | ✅ 零命中 |
| 3. 类实例当字面量传参 | 抽查 repository/model（interface + ValuesBucket 写法） | ✅ 抽查未见违规 |
| 4. 编造 API | 假设 API 清单机制在用（SPEC_M6 §10，4 项中 1 项已核销） | ✅ 机制运转中，余 3 项随 T5/T9 核销 |
| 5. 硬编码字符串/颜色/尺寸 | 全仓正则 | ⚠️ 字符串：2 处中文（**BUG-002**）；颜色：约 40 处（**BUG-003**，深色高风险子集 5 处）；尺寸：普遍未 $r()（**OBS-001** 待定口径）。toast 文案全部 $r() ✅ |
| 6. 页面直连 DB | pages/ 下搜 relationalStore/getRdbStore/DatabaseHelper | ✅ 零命中，entry+wearable 页面全走 repository |
| 7. 联动规则只在 PetEngine | 全仓搜 exp 修改点 | ✅ 仅 entry/wearable 两个 PetEngine.ets 修改 exp/level；调用方只调 addExp() |
| 8. V2 状态管理、不混 V1 | 全仓搜 V1 装饰器 | ⚠️ 仅 1 处：PomodoroTimerPage.ets:340 @CustomDialog 内 @State（**BUG-001**） |
| 9. async/await + try-catch | repository try/finally + 上层 catch 计数 | ✅ 架构性成立（repo 上抛、VM/页面兜底，20 文件有 catch）；细节见 OBS-002 |
| 10. 无第三方依赖 | oh-package 及 import 扫描 | ✅ 仅 @kit.* / @ohos.* 官方包；报表环形图为 Canvas 自绘 |

## 登记结果

- 新增 Bug 3 个（P2 × 3）：BUG-001（V1/V2 混用）、BUG-002（硬编码中文）、BUG-003（硬编码颜色-深色风险）→ `work/docs/BUG_LIST.md`，全部指派主程序
- 观察项 3 个：OBS-001 尺寸口径需老板拍板；OBS-002 异常兜底架构确认；OBS-003 通过项存档
- 均不阻塞编译与回归；BUG-003 建议与 T10 深浅色核验结果合并修复，一次改到位

## 给主程序的修复建议排序

1. BUG-001：C1-C3 特色开发（8/5 开工）前顺手修——@CustomDialog 换 V2 兼容写法或改 promptAction.openCustomDialog，5 分钟级
2. BUG-002：两处 ' 天' 提到 string.json，5 分钟级
3. BUG-003：等 T10 深浅色核验回喂实际视觉表现后按清单批量改色值（避免盲改两遍）
